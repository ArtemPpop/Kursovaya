<UserControl x:Class="Kursovaya.View.FilmsPage" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kursovaya"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Все фильмы" FontSize="20" Foreground="White" Margin="0,0,0,20"/>

            <!-- Контейнер для карточек фильмов -->
            <ItemsControl x:Name="FilmsContainer">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Шаблон карточки фильма -->
                        <Border Width="180" Height="300" Background="#1f1f1f" Margin="10" 
                                CornerRadius="5" Cursor="Hand">
                            <StackPanel>
                                <Image Source="{Binding ImageUrl}" Height="200" Stretch="UniformToFill"/>
                                <StackPanel Margin="10">
                                    <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Genre}" Foreground="#aaa" FontSize="12"/>
                                    <TextBlock Text="{Binding Year}" Foreground="#aaa" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>